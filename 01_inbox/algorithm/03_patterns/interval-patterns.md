---
title: interval-patterns
tags: [algorithm, pattern, interval, sweep-line, scheduling]
aliases: [구간 패턴, 구간 병합, 구간 겹침, Interval Merge]
date modified: 2025-12-18 11:55:01 +09:00
date created: 2025-12-18 11:55:01 +09:00
---

## Interval Patterns: 겹치고 깎고 합치기

시간이나 좌표상의 **구간(Interval)**을 다루는 문제입니다. 시작점과 끝점이 있는 데이터를 어떻게 효율적으로 처리할지가 핵심입니다.

### 💡 Why it matters (Context)

- **충돌 감지**: 두 일정이 겹치는가? (Calendar Conflict)
- **리소스 최적화**: 최소 몇 개의 회의실이 필요한가?
- **범위 통합**: 쪼개진 데이터 구간들을 하나로 합치기.

---

### 🏢 실무 사례

#### Interval 활용
- **Google Calendar/Outlook**: 일정 등록 시 중복 여부 체크 및 시각화.
- **클라우드 리소스 스케줄링**: 가상 머신(VM)들의 사용 시간을 분석하여 서버 가동 최소화.
- **영상 편집기**: 타임라인에서 클립 간의 겹침 처리 및 레이어 순서 결정.
- **네트워크 방화벽**: 허용/차단 IP 범위(Range)의 병합 및 최적화.
- **데이터베이스**: 레인지 쿼리(Range Query) 결과의 병합 처리.

---

## 🔑 핵심 전략: "일단 정렬하라"

구간 문제는 시작 시각을 기준으로 **정렬**하는 순간 난이도가 50% 이상 내려갑니다.

### Pattern 1: Merge Intervals (구간 병합)
**문제**: 겹치는 모든 구간을 하나로 합치기. 예: `[[1,3],[2,6],[8,10]]` → `[[1,6],[8,10]]`

```python
def merge_intervals(intervals):
    if not intervals: return []
    
    # 1. 시작 시간 기준으로 정렬 ⭐
    intervals.sort(key=lambda x: x[0])
    
    merged = [intervals[0]]
    for current in intervals[1:]:
        last_end = merged[-1][1]
        curr_start, curr_end = current
        
        # 겹치면: 이전 구간의 끝점을 확장
        if curr_start <= last_end:
            merged[-1][1] = max(last_end, curr_end)
        # 안 겹치면: 새로운 구간 추가
        else:
            merged.append(current)
            
    return merged
```

### Pattern 2: Interval Intersection (구간 교집합)
**문제**: 두 리스트의 구간들 중 서로 겹치는 부분만 추출.

```python
def interval_intersection(A, B):
    i = j = 0
    res = []
    
    while i < len(A) and j < len(B):
        # 교집합의 시작점: 둘 중 큰 놈
        # 교집합의 끝점: 둘 중 작은 놈
        lo = max(A[i][0], B[j][0])
        hi = min(A[i][1], B[j][1])
        
        if lo <= hi:
            res.append([lo, hi])
            
        # 끝점이 먼저 끝나는 쪽의 포인터를 이동
        if A[i][1] < B[j][1]:
            i += 1
        else:
            j += 1
    return res
```

### Pattern 3: Meeting Rooms (최소 회의실 개수)
**문제**: 동시에 열리는 회의 중 가장 많은 수 찾기. (**Sweep Line** 기법)

```python
def min_meeting_rooms(intervals):
    starts = sorted([i[0] for i in intervals])
    ends = sorted([i[1] for i in intervals])
    
    res = count = 0
    s_ptr = e_ptr = 0
    
    while s_ptr < len(intervals):
        if starts[s_ptr] < ends[e_ptr]:
            count += 1
            s_ptr += 1
        else:
            count -= 1
            e_ptr += 1
        res = max(res, count)
    return res
```

---

## 🚨 흔한 실수 (Common Mistakes)

1.  **정렬 누락** ❌
    - 구간 문제는 정렬 안 하면 $O(N^2)$이 되거나 로직이 꼬입니다. 거의 90% 확률로 정렬이 선행되어야 합니다.
2.  **경계값 처리 (`<=` vs `<`)**
    - `[1, 2]`와 `[2, 3]`이 겹치는 것으로 볼 것인가 말 것인가? 문제 조건을 반드시 확인하세요.
3.  **정렬 기준 오판**
    - 대부분 시작 시간이 기준이지만, 어떤 문제는 종료 시간 기준 정렬([[02_algorithms/greedy|Greedy 참고]])이 정답일 수 있습니다.
4.  **끝점 확장 시 `max` 누락** ❌
    - `merged[-1][1] = curr_end`라고만 하면, `[1, 10]` 뒤에 `[2, 3]`이 올 때 끝점이 3으로 줄어드는 대참사가 발생합니다. 반드시 `max(last_end, curr_end)`를 사용하세요.

---

## ⚡ 최적화 기법: 스윕 라인 (Sweep Line)

좌표평면 위에 수직선(Line)을 대고 왼쪽에서 오른쪽으로 훑으며(Sweep) 만나는 지점(Event)을 처리하는 방식입니다. 구간 합, 넓이 구하기 등에 매우 강력합니다.

---

---

## 📚 관련 문서
- [[02_algorithms/greedy|그리디 알고리즘]] - 종료 시간 기준 정렬을 통한 회의실 배정 최적화
- [[01_data-structures/heap-and-priority-queue|힙과 우선순위 큐]] - 스윕 라인 알고리즘에서 이벤트 관리에 활용
- [[01_data-structures/linear|배열과 리스트]] - 정렬된 선형 공간에서의 데이터 처리 기법
- [[03_patterns/two-pointers|투 포인터]] - 구간 교집합 탐색 등의 변형 문제 풀이
- [[02_algorithms/divide-and-conquer|분할 정복]] - 구간 관리를 위한 자료구조 설계의 기초
