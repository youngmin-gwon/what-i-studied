# 소스와 빌드
- `repo init/sync` 로 소스를 받는다. `source build/envsetup.sh` 후 `lunch` 로 타깃을 고른다.
- Soong/ninja 가 빌드를 수행한다. 모듈 정의는 Android.bp 에 적는다.
- 빌드 산출물: system/vendor/product 이미지, [boot/vendor_boot](01_inbox/mobile/android/00_foundations/glossary/android-glossary.md#boot-image), target-files.zip(OTA 재료).
