# DI를 쓰면서도 내부에서 직접 생성

```kotlin
@Inject
class Repository {
    private val api = ApiImpl()
}
```

이러면 Metro가 `Api`를 교체하거나 테스트용 fake를 넣을 수 없습니다.

```kotlin
@Inject
class Repository(
    private val api: Api,
)
```
