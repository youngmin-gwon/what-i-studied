# 그래프를 만들었는데 아무 것도 노출하지 않음

```kotlin
@DependencyGraph
interface AppGraph
```

이렇게 하면 밖에서 꺼낼 수 있는 것이 없습니다.

```kotlin
@DependencyGraph
interface AppGraph {
    val repository: BenefitRepository
}
```

`val repository` 같은 accessor가 있어야 graph 밖에서 시작점으로 사용할 수 있습니다.
